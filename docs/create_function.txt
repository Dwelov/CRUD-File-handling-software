
	•	createFile():
This function creates a file only if it has a .csv extension.
It checks the filename’s extension and displays an error if the extension is missing or invalid; otherwise,
it creates the file and confirms success.
	
